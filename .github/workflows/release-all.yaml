name: Release All Modules

on:
  workflow_dispatch:

jobs:
  release:
    uses: ./.github/workflows/release.yaml
    permissions:
      contents: write
      id-token: write
      attestations: write
      pages: write
      actions: read
    with:
      release_all: true
